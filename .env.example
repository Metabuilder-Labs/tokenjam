# Copy to .env (gitignored) and fill in real values. Never commit .env.

# Required by examples/sdk_workloads/ (the OpenAI SDK workload corpus) for
# any real (non --dry-run) run. Read from the environment only; never
# hardcoded, never logged, never written to disk by these scripts.
# The workloads make real, billed OpenAI API calls guarded by a mandatory
# --max-spend ceiling (default $2.00) — see examples/sdk_workloads/README.md.
OPENAI_API_KEY=
