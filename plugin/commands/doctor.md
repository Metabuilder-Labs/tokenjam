---
description: Run TokenJam's health check — config, ingest endpoint, and storage. Equivalent to running `tj doctor`.
allowed-tools: Bash(tj doctor *)
---

## tj doctor
!`tj doctor 2>&1 || echo "tj is not installed. Run /onboard first, or install with npx tokenjam@latest / pipx install tokenjam."`

## Instructions
Summarize the health check above. Call out anything reported as a warning or error, and give the concrete fix it suggests (often `tj doctor --repair` or re-running `/onboard`).
